import:
  - catalog/vm/ubuntu/noble
  - catalog/k3s/defaults

components:
  terraform:
    ubuntu-noble-vm:
      metadata:
        inherits:
          - vm/ubuntu/noble
      vars:
        config:
          name: ubuntu-noble
