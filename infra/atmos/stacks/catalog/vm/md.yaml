import:
  - catalog/vm/defaults

components:
  terraform:
    vm/md:
      metadata:
        type: abstract
        inherits:
          - vm/defaults
      vars:
        config:
          hardware_preset: md
