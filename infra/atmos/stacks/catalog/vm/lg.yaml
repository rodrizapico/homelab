import:
  - catalog/vm/defaults

components:
  terraform:
    vm/lg:
      metadata:
        type: abstract
        inherits:
          - vm/defaults
      vars:
        config:
          hardware:
            core_count: 2
            memory_capacity: 4096
